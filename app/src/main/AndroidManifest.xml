<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quxiangtech.myapplication"
    android:sharedUserId="">

    <!-- 前 4 个权限是读写权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- 最后两个权限是 SD 卡访问权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".TestApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:theme="@style/Theme.MyApplication.Launcher"
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.quxiangtech.JiaGuActivity" />

        <activity android:name="com.quxiangtech.ZRouteTestActivity">

        </activity>
        <activity android:name="com.quxiangtech.plugin.ProxyActivity" />

        <service
            android:name=".MotionDetectorService"
            android:enabled="true"
            android:process=":motion_detector">
            <intent-filter>
                <action android:name="custom.intent.action.motion" />
            </intent-filter>
        </service>

        <service android:name=".RemoteService" />
        <service
            android:name="com.quxiangtech.binder.ServiceManagerService"
            android:process=":service_manager" />
    </application>

</manifest>